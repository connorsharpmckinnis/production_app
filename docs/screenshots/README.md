# Screenshots for README

Drop annotated app screenshots here using the filenames suggested in the root README (e.g. `timeline.png`, `rehearse.png`).

Once a file exists, replace the matching **Screenshot to add** placeholder in `README.md` with a normal Markdown image, for example:

```markdown
![Timeline — main view](docs/screenshots/timeline.png?raw=true)
```
